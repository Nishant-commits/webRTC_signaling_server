{
  "name": "webrtc-codelab",
  "version": "0.0.1",
  "description": "WebRTC codelab",
  "dependencies": {
    "node-static": "^0.7.10",
    "socket.io": "^2.0.4"
 },
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "build": "echo 'No build step required for this project'",
    "test": "echo 'No tests specified'",
    "lint": "echo 'No linting configured'",
    "stop": "pkill -f 'node index.js' || echo 'No server running'",
    "restart": "npm run stop && npm start"
  }
}
